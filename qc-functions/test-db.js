const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // if needed, but let's try default auth
// Actually I don't have the serviceAccountKey.json readily accessible in the project.
